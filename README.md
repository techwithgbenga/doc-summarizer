# doc-summarizer
Intelligent Document Processing &amp; Summarization Pipeline. It ingests documents (PDF, Word, or plain text), extracts structured text, performs section-level summarization using an open-source transformer model, stores metadata in a local database, and provides both a CLI tool and a lightweight Flask web API for querying and retrieving summaries.
